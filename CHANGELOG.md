# 更新日志

> 只写**用户能看到的变化**。技术细节见提交记录。

## 1.1.2（2026-09-18）

**修好「多行注释说明提取不全」（这版的主线）**

- **C# 的 `///` 多行说明块以前只进图一行、甚至完全丢掉**：语法树把 `///` 的每一行各算一个独立注释节点，
  而提取时只取靠近声明的那一行 —— 以 `</summary>` 收尾的块会变成空壳（界面显示「（源码里没有注释说明）」），
  末行有文字的块则被静默截成只剩最后一行。现在整块合并：XML 标签剥掉、行间用空格衔接
  （中文标点 / 破折号后面不补空格）。实测 MuSync：有说明的类型从 81 个涨到 **104 个**（共 106），无一条退化。
  注：受影响的不只 C# —— 凡是「用连续行注释当说明」的语言（Rust 的 `///`、Go / Ruby / PHP 的 `//` `#`）同一改法。

**顺带把「行数」三处口径写清楚**（原来都对，只是没标）

- **整文件 / 类型区间 / 代码行**是三个不同的数：`file` 给的是**整文件**行数、类型（symbol / 详情面板）给的是
  **该类型的区间**、overview 的「规模」是**代码行**；系统分组与命名空间那两处行数 = **各类型区间合计**。
  现在这些地方都写明了口径，README 的字段表也补上了。

## 1.1.1（2026-09-18）

**修两处看得见的毛病**

- **MCP 报给 AI 的快照时间以前是"假的本地时间"**：`overview` 的「数据快照」行把 UTC 直接按本地时间的样子
  写出来（实际 UTC 18:16，却显示成 `18:16`，少了 8 小时），于是 AI 会把快照说成"昨晚 18:16 的"。
  现在如实标注时区：`数据快照：2026-09-17 18:32:37 UTC` —— **只标注、不换算**（读的人可能在别的时区、
  甚至别的机器上）。
- **旧版本的引擎解包目录现在会自己清掉**：以前只清"超过 7 天没碰过"的，而每换一次版本就多留约
  160 MB（完全版）/ 45 MB（精简版）—— 换得勤就永远清不掉，只能手工删。
  现在改成**每次启动**都清一次，只留**当前在用的 + 最近用过的那个**（另一份 exe 或一个 MCP 进程可能
  正指着它）—— 也就是**最多留 2 份，不会随版本数一直涨**；清掉几个目录、腾出多少空间会写在启动日志里。


## 1.1.0（2026-09-17）

**解析引擎换了一代**（这版的主线）

- 语法包从 36 个（能用 32 个）换成 **105 个全部可用** —— 以后加语言不再受"能不能加载"限制；
- 单门语法占内存从约 150–180 MB 降到约 **11 MB**；
- **C#：不再需要"解析前改写"**。新语法自己就认得原始字符串、主构造函数、`file` 修饰符、`void*`、
  集合表达式、`required` 成员、`scoped ref` 等（12 项实测均 0 个解析错误）。顺带修掉旧改写的毛病：
  名叫 `required` 的变量以前会被改名成 `required_`；
- **Scala**：新语法认得 Scala 3 的 `enum` 了（地图上会多出一个枚举类型）；
- **TLA+** 的语法包改为随工具自带（上游 npm 包里没有现成的）；
- **SystemRDL 补回（27 门）**：上游没有能配新运行时的 wasm，于是我们用 emscripten（emsdk 预编译的
  clang + wasm-ld，不需要 emcc）**自己编了一份**，随工具自带（`vendor/wasm/`）；`.rdl` 的
  addrmap / reg / field 都能上图。
  顺带纠正一条早先的结论：0.27 的 `MIN_COMPATIBLE` 其实是 **13** —— 编出来的 ABI 13 实测能加载能解析；
  当初“0.27 加载不了旧语法包”真正的原因据**构建形态**（不是 SIDE_MODULE），不是 ABI 门槛。

**其它**

- 「没有源码也能扫」的报错说清楚了：指到原生可执行文件（C/C++ 编译，比如多数游戏本体）时，
  不再说"可能不是 .NET 应用"，而是直接讲明白它是什么、只能扫哪三类、Unity 游戏的
  `<游戏名>_Data\Managed\*.dll` 是例外；
- README 新增「扫描时要注意什么」（语言选择、编码、增量前提、先看 unknown/ambiguous 等）；
- **新增 Ruby 支持**（24 门）：`class` 当类型、`module` 当命名空间（真实项目里就靠它分层）；
  `attr_reader / attr_writer / attr_accessor` 认成属性（一次声明多个全记）；`require` / `require_relative` / `load` 与
  `include` / `extend` / `prepend` 连成依赖边（也包括 `class Circle < Shape` 这种继承）。
- **新增 HCL / Terraform 支持**（25 门）：`.tf` / `.tfvars` / `.hcl` / `.nomad`。图上的节点是块
  （resource / data / module / variable / output / locals / terraform），成员是块里的 attribute；
  块之间的引用会连成依赖边（如 `output "ip"` → `resource "aws_instance" "web"`）。
- **新增 GraphQL 支持**（26 门）：`.graphql` / `.graphqls` / `.gql`。type / interface / union / enum / scalar /
  input / schema / directive 都是图上的节点；字段是成员（函数的参数单独算 argument）；`implements`
  与 union 成员连成继承边；SDL 的 `"""..."""` 描述当"说明"显示。
- **新增「界面语言」设置**（启动器工具栏、就在「增量」右边）：一键在 **中文 / English** 之间切，
  立即生效并记入 `launcher.config.json` 的 `Lang`；也可用环境变量 `CODEATLAS_LANG=en` 指定
  （环境变量优先，给不开启动器、直接连 MCP 的人用）。
  这一版**四个面都覆盖到了**：启动器（按钮 / 提示 / 状态栏 / 对话框 / 报错 / 运行日志）、引擎的扫描输出、
  MCP 八个工具的输出、网页地图（本地服务把语言注入页面）。**切语言不用重新扫描**——
  bundle 里存的是中性值，显示层按语言映射（旧 bundle 里写下的中文值也认得）。
- 顺手修掉启动器里两处过时数字（提示里的「23 门」→「26 门」、日志里的「19 门」→「26 门」），
  以及「MCP 配置」拷出的 JSON：现在会带上 `CODEATLAS_LANG`，并且 command 用实际解析到的 node 绝对路径
  （完全版自带 node.exe，以前写死 `node` 靠 PATH，没装 Node 的机器粘过去会连不上）。
- 按钮文案：主按钮「开跑」→ **「扫描」**（向导里的「保存并开跑」→「保存并扫描」）；只是叫法更贴切，行为没变。
- **反编译不再需要你装任何东西（完全版）**：扫 `.dll` / `.exe` 用启动器**内置**的反编译器（ILSpy 引擎，MIT）；
  扫 `.jar` 用**自带的裁剪版 Java 运行时 + cfr**（只含 `java.base` + `java.logging`，从 microsoft-jdk-25 裁出来）。
  精简版不带 Java 运行时（它本来就要求 .NET 9 桌面 + Node），扫 `.jar` 仍需自己装 Java。
- 体积变化：完全版 **82.8 → 104.3 MB**（多出的主要是那份裁剪运行时）；精简版 **5.1 → 10.1 MB**（多了内置反编译库）。
- 修两处看着像 bug 的地方：向导第三步的标题以前会**压住**右边的按钮（中文下也压），现在标题独占一行；
  扫描报告里「开始解析：N 个文件」在只扫一种语言时会**重复打两遍**，现在只打一遍。
- README 与《使用说明》补上「界面语言」说明，并把 MCP 配置示例改成**启动器实际拷出来的样子**
  （`command` 是内置 node 的绝对路径、带 `env.CODEATLAS_LANG`）；顺手校正体积、语法包数、解包目录名等过时描述。
- 新增 **`USAGE.md`** ——《使用说明》的英文版（跟 README 一套：英文 / 中文各一份），两份互相链接，README 里指向中文说明的两处也改过去了。
- 文档整理：两份 README 顶部加「文档地图」（哪个文件看什么）+ `License` 挪到末尾；补上 `atlas` 全局命令
  与「版本号三处一起改」的说明；`package.json` 的 `version` 从 `1.0.0` 跟上 `1.1.0`。
- README 的「没有源码也能扫」按新行为重写（原来写的是"用到时提示装工具"）。
- **AI 接口补强（四个缺口）**：`initialize` 带上 `instructions`（说明书 + 边界声明）；
  `overview` 第一屏给出**扫描根目录**、**数据快照**（生成时间 / 语言 / 单文件上限 / 是否增量）
  和**可信度前提**（未匹配 / 同名歧义计数）；`refs` 与 `file` 在截断时会说明还剩多少条没显示。

- 顺手修了三处**既有的健壮性问题**（代码审查查出来的，都实测复现过）：
  ① `serve` 会被畸形 URL（例如 `/%zz`）**打死整个进程** → 现在回 400，服务继续跑；
  ② 静态文件服务有 `../` 穿越（实测能读到 web 目录外的文件）→ 现在越界一律 404；
  ③ 网页永久链接里的失效参数（`#by=bogus`、聚焦路径不存在）会让**地图整片空白**并配一句误导提示
  → 现在只认合法值，找不到的聚焦自动忽略。

- 修两处配置相关的小坑（第二遍代码审查）：① **分组规则文件带 UTF-8 BOM 就解析失败**（Windows 上 PowerShell 5.1 的
  `Set-Content -Encoding utf8` / VS Code 的“UTF-8 with BOM” / 老版记事本都会加）→ 现在读之前先去 BOM；
  顺带把启动器 `--draft` 写文件时的编码改成**不带 BOM**（原来自己写出来的规则自己读不回去）；
  ② 草拟规则的调色板里有一个颜色重复（第 4 个和第 8 个一样）→ 换掉。

- 两处“更诚实 / 更稳”的改动（代码审查后用户拍板）：
  ① **非 UTF-8 文件现在会被认出来**：以前注释/字符串会静默变成乱码、报告一字不提 → 现在扫描报告、MCP 的
  `overview`、网页顶部都会提示「N 个文件可能不是 UTF-8」（bundles 里新增 `totals.nonUtf8Files`）；
  ② **配置不再抹掉不认识的字段**：`launcher.config.json` 里（比如此后新版本写的）字段，旧版本保存时会原样留住
  （`Config` 加了 `[JsonExtensionData]`）—— 同一目录里跑新旧两个构建时的“配置互踩”；
  ③ **java 报错里的中文路径不再是乱码**：JVM 内部给上 UTF-8 参数，java.exe 启动器那段（`-jar` 打不开之类）
  再按 GBK 兜一次解码（实测：同一场景改前 `赵不言` 是 `�Բ���`，改后正常）。

- 修一个“传了没用”的开关：`--decompiler <反编译器.jar>` 会被自带的那份抢先（实测：传个非 jar 文件也被静静忽略）
  → 现在**显式指定优先**，自带那份只兵底。

（支持的语言：**27 门代码语言** + 5 种文件级格式。）

## 1.0.0（2026-09-17）

首个正式版本。

**相对上一版（还没发出去的那份）的变化**
- 版本号统一成 **1.0.0** —— 之前 exe 里写 0.3.0、地图顶栏写 v0.1.0，现在两处对得上
- **配置文件不再写 BOM** —— 用脚本或别的工具读 `launcher.config.json`，不会再报 JSON 解析错误
- 文档补齐：新增本文件；README 加了「和同名项目的区别」「灵感来源」；去掉指向内部文件的引用

（功能与上一版完全一致。）

**扫什么**
- 24 门代码语言：C# / TypeScript / TSX / JavaScript / Java / Python / Kotlin / Lua / Go / Rust / C / C++ /
  PHP / Swift / Scala / Shell / Zig / Solidity / OCaml / ReScript / TLA+ / SystemRDL / Emacs Lisp / Elixir
  （外加 JSON / YAML / TOML / CSS / HTML 这些文件级格式，默认不扫、要显式勾）
- 源码目录直接扫；`.dll` / `.exe`（含单文件）/ `.jar` 会先反编译再扫
- 反编译产物的标题会注明"无源码注释"，不假装和源码一样

**看什么**
- 四个视图：树形图 / 树状列表 / 依赖图 / 依赖矩阵；配色、分组、代码行或文件数都能切
- 搜索支持**成员名**（搜 `OnPaint` 能找到"谁定义了这个方法"）
- 点方块看检查器：类型信息、成员、依赖、依赖高亮；地址栏是 permalink，可分享

**怎么用**
- **启动器**（`CodeAtlas.exe`，免安装）：工具栏「项目设置…」向导三步走完就能开跑
  （选项目 → 选语言 → 自动草拟一套系统分组规则），配过的项目下次打开不用重配
- 「增量」勾选框：只重新解析改过的文件（默认关＝每次全量）
- 「MCP 配置」按钮：一键把"让 AI 读这个项目"的配置复制到剪贴板

**给 AI 用**
- 内置 MCP 服务，8 个工具：overview / search / symbol / refs / subgraph / file / map / impact
- `map(budget)`：按 token 预算导出骨架；`impact(name, depth)`：改它会影响谁（多跳）
- 接入步骤见《使用说明》第五节

**稳定性**
- 解析按语言分进程：某门语言出问题只丢那一门，不会连累整体
- 本地服务只绑 `127.0.0.1`，不会弹 Windows 防火墙、也不会暴露给局域网
- 出错会说人话；未预期的异常写 `crash.log` 到 exe 旁边，不会无声消失

**两个发行版**
- 完全版 `CodeAtlas.exe`（约 83 MB）：自带 Node 运行时，什么都不用装
- 精简版 `CodeAtlas-lite.exe`（约 5 MB）：需要机器上有 .NET 9 桌面运行时 + Node.js
