# Code Atlas 使用说明

> 一句话：**把这个 exe 拖到任意代码库上，它会把代码扫成一张能看、能搜、能点开的图**——
> 帮你在陌生项目里快速搞清楚"有哪些东西、谁在用谁"。全部在你自己电脑上跑，代码不外传、不联网。

---

## 一、怎么开始（一分钟）

1. **拿到的是哪个文件**：`CodeAtlas.exe`（约 82 MB，免安装版）。放哪都行，桌面、D 盘、U 盘都可以。
2. **双击打开**。第一次启动会花几秒"释放内置引擎"（约 120 MB，只做这一次），然后弹出 **「项目设置」** 向导：

   | 步骤 | 你要做的 |
   | --- | --- |
   | ① 选项目 | 点「选择文件夹…」选一个代码目录（也可以选 `.dll` / `.exe` / `.jar`，它会先反编译再扫） |
   | ② 选语言 | 默认「自动」就行（所有代码语言都扫）；想快一点就只勾这个项目用的语言 |
   | ③ 分组规则 | 它**按目录结构自动草拟**一套"系统分组"（比如 Utils / Players / UI…）。不想要的取消勾选，名字和颜色可以改。 |

3. 点 **「保存并扫描」**。扫完地图会自动出现在同一个窗口里。
4. **以后再看这个项目：直接点「扫描」** —— 语言和分组规则都记着了，不用再配。

> 想给另一个项目配：点工具栏的 **「项目设置…」** 重新走一遍（每个项目各记各的）。

---

## 二、怎么看这张图

左边是筛选，中间是地图，右边是"详情"。

| 想干什么 | 怎么做 |
| --- | --- |
| 换一种看法 | 左下「视图」：**树形图**（面积=代码量）/ **树状列表** / **依赖图**（谁连谁）/ **依赖矩阵**（模块互相引用次数） |
| 按模块看 | 「分组方式」：系统（你草拟的规则）/ 目录 / 命名空间 / 平铺；「分组层级」1~4 层 |
| 找东西 | 顶部搜索框，**类型名和成员名都能搜**——比如搜 `OnPaint`，能直接找到"谁定义了这个方法" |
| 看某个东西 | 点地图上的方块 → 右边检查器显示：它是什么、在哪个文件哪一行、有哪些成员、依赖了谁、被谁依赖 |
| 看"谁引用谁" | 勾上「依赖高亮」，或者选中一个方块看它连到哪些 |
| 分清颜色 | 「着色」：按文件 / 按类型 / 按依赖深浅 |
| 太乱 | 「最小代码行」滑块滤掉小碎片；「类型类别」只留 class / interface 之类 |
| 分享某个视图 | 地址栏就是完整状态（`#q=OnPaint&v=graph`…），复制给别人能直接打开同样的画面 |

小提示：地图里**暗色方块**是当前的筛选/搜索之外的东西，不是出错。

---

## 三、可能会遇到的

- **Windows 弹"已保护你的电脑 / 未知发布者"**：因为这个 exe 没有买数字签名证书。点「更多信息」→「仍要运行」即可。
  （完全版是自包含的，不需要装 .NET；同目录的 `CodeAtlas-lite.exe` 是精简版，要求电脑上装了 .NET 9 和 Node.js，一般用不上。）
- **第一次启动慢几秒**：那是在释放内置引擎到 `%LocalAppData%\CodeAtlas\engine\`。**只做一次**；删掉那个目录它会自己重建。
- **你项目里的代码会被改动吗**：不会。它只读代码；分组规则默认写在 `%LocalAppData%\CodeAtlas\configs\` 里
  （向导第三步勾了"写进项目目录"才会在你项目里生成一个 `atlas.facets.json`）。
- **图上有些数字带"未知/歧义"**：它对依赖的判断是**按名字静态匹配**的，同名、动态调用这类情况它不装懂——
  界面上凡是没把握的都标出来了（未解析、歧义、置信度）。
- **想卸载**：删掉 exe；再删 `%LocalAppData%\CodeAtlas` 就彻底干净了。
- **扫不动 / 特别慢**：把「项目设置」里的语言勾少一点（只留这个项目真用的），大仓库会快很多。
- **改了几行代码，想再看一眼地图**：不用重新等全量——勾上工具栏的 **「增量」**（见第六节）。

---

## 四、它是什么、不是什么

**是**：一个"看图工具"。把你自己的代码库（或别人的开源库、反编译出来的程序集）扫成中间数据，
让你**自己翻、自己搜、自己点开看**——理解陌生代码的第一步。

**不是**：不是反编译器、不是 IDE、不是"AI 问答"。它不会假装权威：所有推断都标了来源和不确定的地方；
它也不会把代码上传到任何服务器。

支持的语言：C# / TypeScript / TSX / JavaScript / Java / Python / Kotlin / Lua / Go / Rust / C / C++ / PHP /
Swift / Scala / Shell / Zig / Solidity / OCaml / ReScript / Ruby / HCL（Terraform） / GraphQL / TLA+ / Emacs Lisp / Elixir（共 26 门；SystemRDL 暂时缺席，等它的语法包补上），
外加 JSON / YAML / TOML / CSS / HTML 这些文件级格式（默认不扫，要在「项目设置 → 语言」里显式勾）。

---

## 五、给 AI 用（可选，但很省 token）

Code Atlas 自带一个 **MCP 服务**：接上以后，AI（Chatbox / Claude / Cursor / Windsurf / Cline…）
能直接"查"这个代码库，而不是靠你把文件一个个塞进对话——**省几十倍 token，而且答案有出处**。
服务读的是本地那份扫描结果，**不会重扫、不上传任何东西**。

### 5.1 两步接上（推荐）

1. 启动器里对这个项目**点一次「扫描」**（扫出 `dist/bundle.json`）；
2. 点工具栏上的 **「MCP 配置」** → 提示"已复制到剪贴板" → 把那段 JSON **粘进 AI 客户端的 MCP 配置**里 → 重启客户端。

复制出来的内容长这样（路径都是**绝对路径**，不用改）：

```json
{
  "mcpServers": {
    "code-atlas": {
      "command": "node",
      "args": ["C:\\path\\to\\CodeAtlas\\src\\cli.mjs", "mcp", "--out", "C:\\path\\to\\CodeAtlas\\dist"]
    }
  }
}
```

> 完全版用户注意：`command` 里那个 `node` 是**系统 PATH 上的 Node**。
> 如果对方电脑没装 Node.js，把 `command` 换成启动器解包出来的 `node.exe` 绝对路径
> （在 `%LocalAppData%\CodeAtlas\engine\` 下面某个版本目录里）也能用——完全版自带 Node，不缺东西。

### 5.2 各客户端粘在哪

| 客户端 | 位置 |
| --- | --- |
| **Chatbox** | 设置 → MCP / 扩展 → 添加服务器（粘 `mcpServers` 片段） |
| **Claude Desktop** | `%APPDATA%\Claude\claude_desktop_config.json` 里的 `mcpServers` 字段 |
| **Cursor** | 设置 → MCP → Add new MCP server（或 `~/.cursor/mcp.json`） |
| **Windsurf / Cline / 其它** | 找 "MCP servers" 配置项，粘 `mcpServers` 片段 |

粘完**重启客户端**，应该能看到 `code-atlas` 这个 server 和它的 8 个工具。

### 5.3 接上之后能问什么（8 个工具）

| 工具 | 什么时候用 |
| --- | --- |
| `overview` | 先看全局：有多少东西、常见入口是什么 |
| `search(query, scope?)` | 找符号，**默认连成员名一起搜**（"谁定义了 `OnPaint`？"） |
| `symbol(name)` | 某个类型的细节：成员、基类、被引用次数 |
| `refs(name, dir?)` | 谁引用它 / 它引用谁 |
| `subgraph(name, depth)` | 某处的依赖子图 |
| `map(budget)` | **按 token 预算**导出一份骨架（系统 → 关键类型 → 关键成员） |
| `impact(name, depth)` | **影响面**：改它会影响谁（多跳展开） |
| `file(path)` | 某个文件里的类型清单 |

推荐套路：**先 `map` 拿全局 → 再 `search` 定位 → 最后 `symbol` / `refs` / `impact` 深入**，
比让 AI 一个文件一个文件读要省得多。

### 5.4 它不知道的事（诚实说明）

- 依赖关系是**按名字静态匹配**出来的：动态调用、反射、字符串拼出来的名字**看不见**；
- `map` / `overview` 会写明"有多少处引用没匹配上（unknown）/ 匹配到多个（ambiguous）"——那是真的不确定；
- 想让答案跟着代码走：改完代码**重新扫一次**（勾「增量」很快），服务会自动读新结果。

### 5.5 排错

| 现象 | 原因 / 处理 |
| --- | --- |
| 客户端里看不到 `code-atlas` | 配置没粘对位置，或没重启客户端；JSON 里**反斜杠要转义**（`\\`）——启动器复制的那份已经转义好了 |
| 连上了但答"没数据" | `--out` 指错目录，或那个项目还没扫过（先「扫描」一次） |
| 报 `node` 找不到 | 系统没装 Node.js：把 `command` 改成内置 `node.exe` 的绝对路径 |
| 改了代码 AI 还是旧答案 | 重新扫一次（勾「增量」），服务会自动重载 |

> 不接 AI 也完全没关系：这个工具主要是给**你自己翻图**用的。

---

## 六、一个省时间的开关：增量

工具栏上有个 **「增量」** 勾选框（默认关 = 每次全量）。勾上之后重新扫描时**只重新解析你改过的文件**，
其余复用上次结果——改几个文件再看图，一两秒就出来。

（"谁引用谁"那部分仍会整体重算，所以是大项目才明显；输出目录里会多一个缓存文件，删了也没事，下次自动重建。）

## 别踩的两个小坑

- **别把 exe 放进 `C:\Program Files\`（或任何只读位置）** —— 它需要在**自己旁边**写扫描结果（`dist\`）和配置文件；放桌面、D 盘、U 盘都可以。真放进只读目录了，也还能用，只是每次都要重新选；想留下设置就把它挪出来。
- **它只往自己旁边写东西**（`dist\`、`launcher.config.json`），**不会改你的代码**；分组规则默认也存到你自己电脑的用户目录（`%LocalAppData%\CodeAtlas\`）。除非你在向导里主动勾「写进项目目录」，否则被扫描的项目一个字节都不会动。
