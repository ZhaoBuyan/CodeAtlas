# 更新日志

> 只写**用户能看到的变化**。技术细节见提交记录。

## 1.5.0（2026-09-20）

> 说明：1.4.2 打包过、但**没有正式发布**（当时那批内容也并进了本版），所以这里从 1.4.1 直接跳到 1.5.0。

- **修复：同名歧义时跨文件引用被整条丢掉**（`refs` 会答“被谁引用：无”）—— 以前名字解析的优先级是
  “唯一命中 → 同文件 → 同命名空间 → 同根包 → 放弃”，**没有用 import 消歧这一步**；JS 这类没有命名空间的
  语言里，只要同一个名字在两个文件各定义一次，第三方调用就会整条丢掉（自扫实测 **17 条边**）。现在在
  “同文件”之后补了一档：**用引用方文件 import 的是哪个模块来消歧**；定不下来才放弃。
- **修复：命名空间语言（C#/Java/Kotlin）的证据分档整片塔成“仅同名”** —— 以前只拿“被引用文件的**文件名**主干”
  去比对引用方的 import，而命名空间语言 import 的是 `using MuSync.Models;` 这种**命名空间**，永远比不上：
  实测一个 C# 项目 53 条边里“有支撑”**0 条**，连同命名空间互相引用也标成“别当真”，于是 `overview` 的
  “被依赖最多”榜单跟着失真（真正被引 387 次的类型连榜都上不去）。现在 import 分档改走同一套口径：
  **模块名 / 命名空间 / 包路径都能认**，同命名空间也算有支撑。标签从 `[import]` 改成 **`[有支撑]`**。
- **修复：顶层函数的调用位置拿不到**（`refs("startMcp")` 只给到“引用方的声明行”）—— ① 的位置记录在
  父进程里做过一道筛选（只留“是某个成员的名字”，为的是控制体积），但**顶层函数在这套模型里是“类型”不是成员**，
  位置被一起筛掉了。现在保留集改成 **成员名 ∪ 类型名**，并且**类型查询也直接给“调用 / 访问位置”**
  （以前只有成员查询给），于是拿一个函数名就能看到“第几行被调用”。
- **措辞：**“匹配到多个同名目标”以前写“只取了一个”，实际是**没有计入图里**（宁可缺边也不接错）—— 已改准。
- **措辞：**成员回答里 `被引 N 次` 与下面的“位置 N 处”是两个不同口径的数，以前并排容易误读；改成
  `**该类型**被引 N 次`。
- **成员级“调用 / 访问位置”（`refs("方法名")` 直接给 `file:line`）** —— 以前问一个方法名，只能告诉你“它属于哪个类型、
  那个类型被谁引用”（超集，还得自己翻文件）。现在**每个类型额外记下“哪个名字在哪一行出现在调用位 / 成员访问位”**，
  于是 `refs("ClearStatus")` 或 `refs("类型.成员")` 会直接列出位置：
  ```
  调用 / 访问位置（按名字匹配，共 12 处，其中调用 9 处）：
    src/SteamStatusManager.cs:88	调用
    src/MainForm.cs:214	访问
  ```
  口径与边界（工具描述与 instructions 里也写着）：**按名字匹配**，不解析接收者类型 → **同名的其它成员会混进来**；
  **声明处不算调用点**；“调用 / 访问”是按源码判的（名字后面紧跟 `(` = 调用；前面紧挨 `.` / `->` / `::` = 访问）——
  所以 Lisp 那种 `(foo x)` 写法、动态调用 / 别名 / 反射都抓不到；**只记项目里确实存在的成员名**
  （对外部成员的调用，如 `Assert.Equal`，不留位置 —— 那种名字在图里没有成员，也问不到它头上）。
  实测代价：一个真实样本（61 个文件）的 bundle 从 0.16 MB → **0.28 MB**（多 3140 处位置，约 30 字节/处）。
  老图（没这个字段）会明说“**这份图里没有这类记录 —— 重新扫一次就会带上**”，不会把“没记录”说成“没有调用点”。
- **`symbol(name, neighbors: true)`：一个参数把“邻居”带出来（默认关，输出一个字节不变）** ——
  改一个类型时最常接着问的三件事：**被谁引用 / 引用了谁 / 相关测试文件**，现在一次就能拿到
  （各给前 5 个；抬头与 `refs` **同一个口径**：`12 条边 · 共 382 次`。要证据标签与完整列表仍然去 `refs`）。
  **默认不显示**：`symbol` 的输出本来就长（上一轮复测报告嫌它啰嗦），所以只有显式写 `neighbors: true`
  才追加，不带时输出**与以前一模一样**（已拿 16 个类型的全量输出做逐字节对比）。
  相关测试文件沿用 `impact` 那套口径（按路径认），而且“图里没有测试文件”与“有测试文件但都不引用它”分开说。
- **`exclude`：不需要的部分由调用方自己排掉（新增参数，5 个工具）** —— `overview` / `search` / `refs` / `map` / `impact`
  多了一个可选参数，值是一串路径（逗号分隔，如 `exclude: "tests/fixtures, vendor"`），把匹配的名字从**这一次调用**的
  名单里去掉 —— 用完即弃，不落成设置（项目里固定要排除的仍然走 `atlas.ignore` / 扫描范围）。引擎不认识
  “你项目里哪些是样例数据 / 生成物”，所以这个判断交给调用方。规则只有三条、一眼能猜对：**多段项**（`tests/fixtures`）
  按**连续段序列**匹配（命中 `a/tests/fixtures/b.java`，不命中 `tests/x/fixtures/y`）；**单段项**（`vendor`）匹配任意层级的
  目录段**或文件名的主干**（`src/vendor.ts` 也会被排掉）；一律**大小写不敏感**。不做通配、不看扩展名
  （`*.g.cs` 那类属于项目属性，走 `atlas.ignore`）。
  每次排除都在名单**后面当场**写清排掉了多少（`（exclude "tests/fixtures"：候选里排除了 134 个类型）`）；
  名单被整份排空时写**「本次全部被 exclude 排除」**而不是 `（0 个）`；写错字、一条都没命中也会明说。
  **它只影响名单，不改项目事实** —— `overview` 的「规模」那行永远还是整库的数。（`list` / `symbol` / `file`
  不加这个参数：浏览类工具加了会让人误判“这里没有”。）
- **`impact` 单列“会被波及的测试文件”**：改一个类型时，“要跑哪些测试”和“哪些生产代码要改”是两件事，现在分开说 ——
  `会被波及的测试文件（3 个）：tests/app.test.js · tests/util.spec.js · tests/legacy_test.go`。
  测试文件只能**按路径**认（大多数语言里测试就是普通函数，语法树上分不出来）：`test` / `tests` / `__tests__` 目录、
  文件名里的 `.test.` / `.spec.` / `_test.` / `_spec.`、以及 `test_` 开头（Python 惯例）。
  两个**故意不认**的情况：`spec/` 目录（常常是 API 规范而不是测试）、名字里只是碰巧带 test 的（`latest.js`、`tester.js`）。
  代价是 `tests/fixtures/**` 这类**样例语料也会被算进来**（实测本项目自扫时 54 个里 53 个是它）——
  宁可让名单带点噪音，也不在分类器里写一条“本项目的经验规则”。
  名单为空时会说清是哪一种：**“图里认出 N 个测试文件，都不引用它”** / **“图里没有测试文件”** /
  **“图里没有这个标记”**（老版本引擎扫的图就是最后一种 —— 不会把“引擎旧”或“没认出”说成“没有测试会被波及”）。
- **`overview` 会告诉你“这张图是不是旧的”**：它把**已经纳入图里**的文件在磁盘上重新看一遍，变过的就点出来 ——
  `⚠ 快照后 3 个已纳入图里的文件有改动（其中 1 个仅时间戳变化）—— 未反映在图里`。
  **大小变了**才算“内容变了”；**只有时间戳变了**的单独计数，措辞上写“时间戳变化”而不是“未改”——
  等长改动（改常量、等长重命名）本来就看不出大小变化，切分支也会批量刷时间戳，所以证明不了内容没变；
  但两个数分开摆，一眼就知道图的内容大体还准不准、要不要重扫。
  “另有 N 个已不在磁盘”是白送的：图还在、文件没了，图自己永远发现不了（它的引用与影响面会虚高）。
  覆盖面如实限定：只 re-stat 图里已有的文件（**不遍历目录**），所以**新增**的文件它看不见 ——
  但「图里有、磁盘上已经没了」它会点出来（`另有 N 个已不在磁盘`）；
  新增的文件要重新扫描才进图；数字超过 50 截断成 `50+` —— 这行的价值是“图旧了、别全信”，不是文件清单。

- **成员级查询（以前查不了）**：`refs` / `symbol` / `subgraph` / `impact` 现在接受**成员名**（`ClearStatus`）
  或 **`类型.成员`**（`MuSync.SteamStatusManager.ClearStatus`）。以前这种问法会答“找不到匹配的符号”（既不准也没用），
  现在会把**所属类型**（带 `id` 与“被引 N 次 / 其中算数的 M 次”）指出来，并说明下一步怎么找精确调用点。
  依赖边本来就是**类型级**的（成员级调用点没有逐个记录），所以给的是**超集**——这一点现在写进了
  工具描述与 instructions，不再让人误以为“没有引用”。
- **`search` 的成员命中带上所属类型 id 与引用次数**（`（所属类型 id=14，被引 11 次）`），可以直接接 `refs(14)`。
- **行数口径统一**：`overview` 的“最大的文件”写明**按代码行**；`file` 写的是**总行**
  （`1673 行（其中代码 1334 / 注释 203 / 空 136）`）——同一个文件两个数不再让人以为扫错了。

## 1.4.1（2026-09-20）

- **修掉：成员"计了数却列不出来"**。取不到名字的成员以前**只计进分档、不进成员列表** —— 于是 `symbol` 里
  “成员 field 7”却一条不列、`map` 的关键成员整块消失（尤其是 C# 的字段/常量）。现在名字提取多了几层兜底
  （包括“自定义类型的字段要取字段名，不能取类型名”这条），**分档与列表恒定一致**，测试里加了全局断言盯着它。
- **`symbol` 的“共 N”改成真实总数**（以前等于列表长度，两者相等时看不出问题）；成员一个都列不出来时也会说明原因。
- **`refs` 抬头补上“共几次”**：以前只写“几条边”，现在写成 `被谁引用（4 条边 · 共 11 次）`，
  免得只看到条数而误判量级。
- **跳过目录的排序**：项目规则跳掉的排前面并标注，默认表命中的（`.git` / `node_modules` 这类）跟在后面 ——
  不删名字，只是不再把真正需要注意的挤到后面。
- **启动器「扫描范围」的悬浮提示**不再指向不存在的选项名（改为直接写 `.gitignore`，与实际列表项一致）。
- **修掉：「内构监控」其实没在监控**。启动器把 `--watch` 传给了**位置参数**那条路（`<目录> --watch`），而 `--watch` 只在 `scan` 子命令里生效
  —— 于是这个按钮从加进来（`c87134b`）起就一直是“扫描 + 起服务”，改代码不会自动更新。现在启动器监控时走 `scan`。
  实测（本地源码目录、12 个文件）：改一个文件 → **新 bundle 落盘 0.3–1.6 秒**（引擎每 1.5 秒巡检一次 + 增量重扫），
  页面在下一次轮询（每 2.5 秒）里重绘，**端到端约 1.5–4 秒**。
- **MCP 时效提醒（只在文档里说一句）**：MCP 客户端不会自动重载服务器 —— 升级 CodeAtlas 后请**重启客户端**；
  数据本身是每次调用现读的，重扫不必重连。

## 1.4.0（2026-09-20）

- **扫描范围**：工具栏的「语言」按钮改名「扫描范围」；列表最后多了一项 **`.gitignore`**（勾上 = 按项目自己的 .gitignore 跳过，默认不勾）。
- **自选跳过**：扫描目标根放一份 **`atlas.ignore`**（目录名 / `目录名/` / 通配 / `#` 注释）就能按项目自己选要跳什么；
  命令行对应 `--gitignore`（按项目自己的 `.gitignore` 跳，**含各个子目录自带的那份**——口径跟 git 一致：
  每份规则只管它所在的那棵子树，进不去的目录不再往下走）。**跳过了什么一律进扫描报告**
  （读了几个规则文件也会写出来；`!` 例外暂不支持，会计数并提示）。
- **缩放 / 平移**：滚轮放大缩小、**右键拖动移动画布**（左键仍归选中/钻取）、右下角新增 **「回正」**；
  点选模块**不再把倍率跳回 1×**。
- **成员签名**：`symbol` / `search` / 网页检查器里的成员现在带**参数表与返回类型**（如 `area(int, int): double`，
  属性就是它的类型）——同名重载不再长得一模一样。JS/TS 的顶层函数、C#/Java 的 `record` 主构造函数
  这类"类型自己带参数"的也在类型那一行显示。语法树里取不到就什么都不写（**空着 = 没抽到，不是"没有参数"**）。
- **引用次数（边权重）**：依赖边的权重以前只是"有或没有"（总是 1），现在是真的**引用次数** ——
  同一个类型里同一个名字出现几次就是几（以前去重丢了）。连带效果：
  · 地图上的**连线**：**一条引用画一根线**（A 被 B、C 各引用 3 次 → 从 A 中心散出 6 根，B 三根 C 三根），
  根数直接就是次数；**两端都落在两个模块的中心**（不落到模块外面），靠中段弓起的方向分开；
  抬头会写“连线 N 根 = 引用次数”；依赖矩阵的浓淡、检查器里每条边的 `×N` 也是真的次数；
  · `symbol` / `overview` / `search` 里的"被引用多少次"也是加权的（以前是"有多少个来源"，两者正好相等）；
  · 引用列表的抬头会同时写"几条边 · 共几次"。
- **抬头多一个「跳过」chip**：网页顶部现在会写「跳过 N 类目录（M 个） · 规则跳了 K 个」，
  悬停列出是哪几个目录、项目规则来自哪个文件 —— 以前"图里少了东西"只在命令行报告与 MCP 里能看到，
  盯着地图看不出来。没跳过任何东西时不显示（老 bundle 没这个字段也一个字不说）。
- **修掉**：中文注释/名称溢出色块（按真实字宽截断）；分组标题带只在装得下标题时留（小分组不再留空带）；
  地图外面右键也弹系统菜单；同名符号误归（本地声明优先）；C / C++ 的成员名不再把参数表拼进名字里
  （`shape_area(const Shape *s)` → `shape_area`）。
- **顺带修掉的报错 / 自检问题**（一通通读自查找出来的）：
  · 英文界面下扫描报告里的“**…共 N 种**”是中文（现在按语言拼成 “… N in total”）；
  · `atlas serve` 找不到 bundle 时直接甩 Node 堆栈（现在一句人话：“找不到 …，先扫描一次”）；
  · **启动器无界面自检**：以前引擎扫挂了退出码也是 0（脚本/CI 会当成成功）→ 现在失败给非 0；
    参数写错（缺值 / `--port abc` / 不认识的选项）以前直接崩或静默忽略 → 现在写一行人话并按失败计；
    自检以前会卡满 10 分钟（它走的是“扫描 + 起服务”那条永不自退的路）→ 改走 `scan`，扫完即返。

## 1.3.1（2026-09-20）

**新增**

- **git 热度着色**：按文件的 git 改动次数着色，一眼看出最近改得最多的地方；有未提交改动的文件带亮边框。
- **内构快照 / 内构监控**（启动器双态按钮）：切到「内构监控」后，你在编辑器里改代码，浏览器里那张图
  **自己更新**（增量重扫，只重新解析改动过的文件），不用重扫、也不用刷新页面。
- **地图右下角「取消选中」**：点一下清掉选中，右侧详情跟着清空（选中项也从网址里去掉）。
- 色块够大时，块里会多印一行**该符号的注释摘要**。

**修复**

- **同名误判不再和真引用混在一起**：`refs` 每条都标出证据强度（同文件 / import / 仅同名），
  `overview` 的热点榜改按「有证据的引用」排序。
- **分节线不再被当成说明**：`─ ━ ═ ▬` 这类框线以前会被抽成文档，现在不当作说明。
- **鼠标停在模块名字上算命中该模块**：以前文字会吃掉鼠标事件，悬停提示和点击都落到文字上、不落到模块。
- 改网页相关文件后，**已打开的页面会自己重载**，不用再手动刷新。
- 文案清理：去掉下拉里多余的解释、悬浮提示改成正式措辞。

## 1.3.0（2026-09-18）

**结果修正：两处会“静默少东西”的默认规则**（拿真实 monorepo 压出来的）

- **`packages/` 不再被当构建目录跳过**：它是 pnpm / yarn workspaces / lerna / Nx / Turborepo 的源码根，
  以前扫这类 monorepo 会得到一张近乎空白的地图、**还不报错**。实测一个 Electron 项目：修前 **4 文件 / 319 行**，
  修后 **217 文件 / 20,116 行**。
- **机器生成的锁文件不再被当代码扫**：`pnpm-lock.yaml` / `package-lock.json` / `yarn.lock` / `*.lock` / `go.sum` …
  以及 `*.g.dart` / `*.snap` / `*.map`。同一个项目全选语言时，一个 `pnpm-lock.yaml` 曾吃掉整张图 97% 的“代码行”。
- **被跳过的目录会点名**：扫描报告与 `overview` 新增一行「跳过目录 node_modules 312 · dist 4 …」，
  免得“图里少了东西”只能靠猜。
- **升级后新语言不再静默缺席**：启动器保存的是语言 id 列表，以前“全选”过的配置在新增语言后只会勾中旧的那些
  （实测：27/32 项的旧配置升级后看不到 `.vue`）。现在：若已保存的选择本来就是全选（代码语言一个不落），
  新语言会**自动补上**；只勾代码语言时，点一次“确定”会把保存值归一化成「自动（所有代码语言）」，
  以后新增语言自动跟上；若同时勾了文件级格式（json / yaml …），则存为**显式全量列表**（其中已含新语言）。
  （打开一次语言窗口即生效；不想点的话可以等下一版把“加载时就地判定”补上。）

**新语言：Vue（`.vue`）**

- 只解析 `<script>` / `<script setup>`（模板与样式不参与），语法借 TSX —— **不引入 vue 语法包**（它在本运行时里一解析就 abort）。
- 预处理按标签边界切、逐字节对齐，所以**行号 / 注释 / `file:line` 仍然指回 `.vue` 原文**（CRLF 与跨行开始标签都验过）。
- `defineProps` 这类编译宏会如实算成“未解析引用”，不特殊处理。

**文档**：支持语言 27 → 28；「默认跳过什么」写全（目录 + 生成物清单 + 报告会点名）；`packages/` 这个反例点明。

## 1.2.0（2026-09-18）

**MCP 多了一个工具：`list`（按目录浏览）**

- `list(path?, limit?)`：不传路径就列扫描根，传了就列那一层的目录 / 文件（带文件数、类型数、行数）——
  **还不知道任何名字时从这里起手**，它给出的路径与文件名可以直接喂给 `file()` / `search()`。
  以前 AI 面对陌生代码库只能靠猜名字。（工具数 8 → 9）

**顺手补强四处（都是“少一次往返 / 少一次误判”）**

- `file` 的类型清单现在印**限定名**（fqn）—— 从“这个文件”跳到“这个符号”不再断链；
- `overview` 的解析异常不再只给数量，**直接列出是哪几个文件**（`路径(次数)`）；
- 每个工具结果末尾挂一条**快照时间（UTC）** —— 只调一个工具也能看出数据新不新；
- `symbol` 新增 `members=` 参数（默认 40 / 上限 300），成员多的时候能一次取全。

**文档**：README / 使用说明里“8 个工具”改成 9，工具表补上 `list`，并写清各工具的截断阈值与快照时间。

## 1.1.3（2026-09-18）

**两个小补丁（都跟“看数据更省事”有关）**

- **搜索命中带一行说明**：`search` 的类型 / 成员命中后面会跟一句该符号的说明摘要（截到 80 字）——
  多数情况不用再为看一眼注释专门调 `symbol`，省一轮往返；想要全文再用 `symbol`。
- **`subgraph` 每层的截断写明了**：每层最多列 40 个符号（控 token），以前表头写的是全集数量，
  看着像“这层只有 40 个”；现在超限时写「第 2 层（前 40 / 共 49）」，与 `symbol` 成员表口径一致。

## 1.1.2（2026-09-18）

**修好「多行注释说明提取不全」（这版的主线）**

- **C# 的 `///` 多行说明块以前只进图一行、甚至完全丢掉**：语法树把 `///` 的每一行各算一个独立注释节点，
  而提取时只取靠近声明的那一行 —— 以 `</summary>` 收尾的块会变成空壳（界面显示「（源码里没有注释说明）」），
  末行有文字的块则被静默截成只剩最后一行。现在整块合并：XML 标签剥掉、行间用空格衔接
  （中文标点 / 破折号后面不补空格）。实测 MuSync：有说明的类型从 81 个涨到 **104 个**（共 106），无一条退化。
  注：受影响的不只 C# —— 凡是「用连续行注释当说明」的语言（Rust 的 `///`、Go / Ruby / PHP 的 `//` `#`）同一改法。

**顺带把「行数」三处口径写清楚**（原来都对，只是没标）

- **整文件 / 类型区间 / 代码行**是三个不同的数：`file` 给的是**整文件**行数、类型（symbol / 详情面板）给的是
  **该类型的区间**、overview 的「规模」是**代码行**；系统分组与命名空间那两处行数 = **各类型区间合计**。
  现在这些地方都写明了口径，README 的字段表也补上了。

## 1.1.1（2026-09-18）

**修两处看得见的毛病**

- **MCP 报给 AI 的快照时间以前是"假的本地时间"**：`overview` 的「数据快照」行把 UTC 直接按本地时间的样子
  写出来（实际 UTC 18:16，却显示成 `18:16`，少了 8 小时），于是 AI 会把快照说成"昨晚 18:16 的"。
  现在如实标注时区：`数据快照：2026-09-17 18:32:37 UTC` —— **只标注、不换算**（读的人可能在别的时区、
  甚至别的机器上）。
- **旧版本的引擎解包目录现在会自己清掉**：以前只清"超过 7 天没碰过"的，而每换一次版本就多留约
  160 MB（完全版）/ 45 MB（精简版）—— 换得勤就永远清不掉，只能手工删。
  现在改成**每次启动**都清一次，只留**当前在用的 + 最近用过的那个**（另一份 exe 或一个 MCP 进程可能
  正指着它）—— 也就是**最多留 2 份，不会随版本数一直涨**；清掉几个目录、腾出多少空间会写在启动日志里。


## 1.1.0（2026-09-17）

**解析引擎换了一代**（这版的主线）

- 语法包从 36 个（能用 32 个）换成 **105 个全部可用** —— 以后加语言不再受"能不能加载"限制；
- 单门语法占内存从约 150–180 MB 降到约 **11 MB**；
- **C#：不再需要"解析前改写"**。新语法自己就认得原始字符串、主构造函数、`file` 修饰符、`void*`、
  集合表达式、`required` 成员、`scoped ref` 等（12 项实测均 0 个解析错误）。顺带修掉旧改写的毛病：
  名叫 `required` 的变量以前会被改名成 `required_`；
- **Scala**：新语法认得 Scala 3 的 `enum` 了（地图上会多出一个枚举类型）；
- **TLA+** 的语法包改为随工具自带（上游 npm 包里没有现成的）；
- **SystemRDL 补回（27 门）**：上游没有能配新运行时的 wasm，于是我们用 emscripten（emsdk 预编译的
  clang + wasm-ld，不需要 emcc）**自己编了一份**，随工具自带（`vendor/wasm/`）；`.rdl` 的
  addrmap / reg / field 都能上图。
  顺带纠正一条早先的结论：0.27 的 `MIN_COMPATIBLE` 其实是 **13** —— 编出来的 ABI 13 实测能加载能解析；
  当初“0.27 加载不了旧语法包”真正的原因据**构建形态**（不是 SIDE_MODULE），不是 ABI 门槛。

**其它**

- 「没有源码也能扫」的报错说清楚了：指到原生可执行文件（C/C++ 编译，比如多数游戏本体）时，
  不再说"可能不是 .NET 应用"，而是直接讲明白它是什么、只能扫哪三类、Unity 游戏的
  `<游戏名>_Data\Managed\*.dll` 是例外；
- README 新增「扫描时要注意什么」（语言选择、编码、增量前提、先看 unknown/ambiguous 等）；
- **新增 Ruby 支持**（24 门）：`class` 当类型、`module` 当命名空间（真实项目里就靠它分层）；
  `attr_reader / attr_writer / attr_accessor` 认成属性（一次声明多个全记）；`require` / `require_relative` / `load` 与
  `include` / `extend` / `prepend` 连成依赖边（也包括 `class Circle < Shape` 这种继承）。
- **新增 HCL / Terraform 支持**（25 门）：`.tf` / `.tfvars` / `.hcl` / `.nomad`。图上的节点是块
  （resource / data / module / variable / output / locals / terraform），成员是块里的 attribute；
  块之间的引用会连成依赖边（如 `output "ip"` → `resource "aws_instance" "web"`）。
- **新增 GraphQL 支持**（26 门）：`.graphql` / `.graphqls` / `.gql`。type / interface / union / enum / scalar /
  input / schema / directive 都是图上的节点；字段是成员（函数的参数单独算 argument）；`implements`
  与 union 成员连成继承边；SDL 的 `"""..."""` 描述当"说明"显示。
- **新增「界面语言」设置**（启动器工具栏、就在「增量」右边）：一键在 **中文 / English** 之间切，
  立即生效并记入 `launcher.config.json` 的 `Lang`；也可用环境变量 `CODEATLAS_LANG=en` 指定
  （环境变量优先，给不开启动器、直接连 MCP 的人用）。
  这一版**四个面都覆盖到了**：启动器（按钮 / 提示 / 状态栏 / 对话框 / 报错 / 运行日志）、引擎的扫描输出、
  MCP 八个工具的输出、网页地图（本地服务把语言注入页面）。**切语言不用重新扫描**——
  bundle 里存的是中性值，显示层按语言映射（旧 bundle 里写下的中文值也认得）。
- 顺手修掉启动器里两处过时数字（提示里的「23 门」→「26 门」、日志里的「19 门」→「26 门」），
  以及「MCP 配置」拷出的 JSON：现在会带上 `CODEATLAS_LANG`，并且 command 用实际解析到的 node 绝对路径
  （完全版自带 node.exe，以前写死 `node` 靠 PATH，没装 Node 的机器粘过去会连不上）。
- 按钮文案：主按钮「开跑」→ **「扫描」**（向导里的「保存并开跑」→「保存并扫描」）；只是叫法更贴切，行为没变。
- **反编译不再需要你装任何东西（完全版）**：扫 `.dll` / `.exe` 用启动器**内置**的反编译器（ILSpy 引擎，MIT）；
  扫 `.jar` 用**自带的裁剪版 Java 运行时 + cfr**（只含 `java.base` + `java.logging`，从 microsoft-jdk-25 裁出来）。
  精简版不带 Java 运行时（它本来就要求 .NET 9 桌面 + Node），扫 `.jar` 仍需自己装 Java。
- 体积变化：完全版 **82.8 → 104.3 MB**（多出的主要是那份裁剪运行时）；精简版 **5.1 → 10.1 MB**（多了内置反编译库）。
- 修两处看着像 bug 的地方：向导第三步的标题以前会**压住**右边的按钮（中文下也压），现在标题独占一行；
  扫描报告里「开始解析：N 个文件」在只扫一种语言时会**重复打两遍**，现在只打一遍。
- README 与《使用说明》补上「界面语言」说明，并把 MCP 配置示例改成**启动器实际拷出来的样子**
  （`command` 是内置 node 的绝对路径、带 `env.CODEATLAS_LANG`）；顺手校正体积、语法包数、解包目录名等过时描述。
- 新增 **`USAGE.md`** ——《使用说明》的英文版（跟 README 一套：英文 / 中文各一份），两份互相链接，README 里指向中文说明的两处也改过去了。
- 文档整理：两份 README 顶部加「文档地图」（哪个文件看什么）+ `License` 挪到末尾；补上 `atlas` 全局命令
  与「版本号三处一起改」的说明；`package.json` 的 `version` 从 `1.0.0` 跟上 `1.1.0`。
- README 的「没有源码也能扫」按新行为重写（原来写的是"用到时提示装工具"）。
- **AI 接口补强（四个缺口）**：`initialize` 带上 `instructions`（说明书 + 边界声明）；
  `overview` 第一屏给出**扫描根目录**、**数据快照**（生成时间 / 语言 / 单文件上限 / 是否增量）
  和**可信度前提**（未匹配 / 同名歧义计数）；`refs` 与 `file` 在截断时会说明还剩多少条没显示。

- 顺手修了三处**既有的健壮性问题**（代码审查查出来的，都实测复现过）：
  ① `serve` 会被畸形 URL（例如 `/%zz`）**打死整个进程** → 现在回 400，服务继续跑；
  ② 静态文件服务有 `../` 穿越（实测能读到 web 目录外的文件）→ 现在越界一律 404；
  ③ 网页永久链接里的失效参数（`#by=bogus`、聚焦路径不存在）会让**地图整片空白**并配一句误导提示
  → 现在只认合法值，找不到的聚焦自动忽略。

- 修两处配置相关的小坑（第二遍代码审查）：① **分组规则文件带 UTF-8 BOM 就解析失败**（Windows 上 PowerShell 5.1 的
  `Set-Content -Encoding utf8` / VS Code 的“UTF-8 with BOM” / 老版记事本都会加）→ 现在读之前先去 BOM；
  顺带把启动器 `--draft` 写文件时的编码改成**不带 BOM**（原来自己写出来的规则自己读不回去）；
  ② 草拟规则的调色板里有一个颜色重复（第 4 个和第 8 个一样）→ 换掉。

- 两处“更诚实 / 更稳”的改动（代码审查后用户拍板）：
  ① **非 UTF-8 文件现在会被认出来**：以前注释/字符串会静默变成乱码、报告一字不提 → 现在扫描报告、MCP 的
  `overview`、网页顶部都会提示「N 个文件可能不是 UTF-8」（bundles 里新增 `totals.nonUtf8Files`）；
  ② **配置不再抹掉不认识的字段**：`launcher.config.json` 里（比如此后新版本写的）字段，旧版本保存时会原样留住
  （`Config` 加了 `[JsonExtensionData]`）—— 同一目录里跑新旧两个构建时的“配置互踩”；
  ③ **java 报错里的中文路径不再是乱码**：JVM 内部给上 UTF-8 参数，java.exe 启动器那段（`-jar` 打不开之类）
  再按 GBK 兜一次解码（实测：同一场景改前 `中文` 是 `�Բ���`，改后正常）。

- 修一个“传了没用”的开关：`--decompiler <反编译器.jar>` 会被自带的那份抢先（实测：传个非 jar 文件也被静静忽略）
  → 现在**显式指定优先**，自带那份只兵底。

（支持的语言：**27 门代码语言** + 5 种文件级格式。）

## 1.0.0（2026-09-17）

首个正式版本。

**相对上一版（还没发出去的那份）的变化**
- 版本号统一成 **1.0.0** —— 之前 exe 里写 0.3.0、地图顶栏写 v0.1.0，现在两处对得上
- **配置文件不再写 BOM** —— 用脚本或别的工具读 `launcher.config.json`，不会再报 JSON 解析错误
- 文档补齐：新增本文件；README 加了「和同名项目的区别」「灵感来源」；去掉指向内部文件的引用

（功能与上一版完全一致。）

**扫什么**
- 24 门代码语言：C# / TypeScript / TSX / JavaScript / Java / Python / Kotlin / Lua / Go / Rust / C / C++ /
  PHP / Swift / Scala / Shell / Zig / Solidity / OCaml / ReScript / TLA+ / SystemRDL / Emacs Lisp / Elixir
  （外加 JSON / YAML / TOML / CSS / HTML 这些文件级格式，默认不扫、要显式勾）
- 源码目录直接扫；`.dll` / `.exe`（含单文件）/ `.jar` 会先反编译再扫
- 反编译产物的标题会注明"无源码注释"，不假装和源码一样

**看什么**
- 四个视图：树形图 / 树状列表 / 依赖图 / 依赖矩阵；配色、分组、代码行或文件数都能切
- 搜索支持**成员名**（搜 `OnPaint` 能找到"谁定义了这个方法"）
- 点方块看检查器：类型信息、成员、依赖、依赖高亮；地址栏是 permalink，可分享

**怎么用**
- **启动器**（`CodeAtlas.exe`，免安装）：工具栏「项目设置…」向导三步走完就能开跑
  （选项目 → 选语言 → 自动草拟一套系统分组规则），配过的项目下次打开不用重配
- 「增量」勾选框：只重新解析改过的文件（默认关＝每次全量）
- 「MCP 配置」按钮：一键把"让 AI 读这个项目"的配置复制到剪贴板

**给 AI 用**
- 内置 MCP 服务，8 个工具：overview / search / symbol / refs / subgraph / file / map / impact
- `map(budget)`：按 token 预算导出骨架；`impact(name, depth)`：改它会影响谁（多跳）
- 接入步骤见《使用说明》第五节

**稳定性**
- 解析按语言分进程：某门语言出问题只丢那一门，不会连累整体
- 本地服务只绑 `127.0.0.1`，不会弹 Windows 防火墙、也不会暴露给局域网
- 出错会说人话；未预期的异常写 `crash.log` 到 exe 旁边，不会无声消失

**两个发行版**
- 完全版 `CodeAtlas.exe`（约 83 MB）：自带 Node 运行时，什么都不用装
- 精简版 `CodeAtlas-lite.exe`（约 5 MB）：需要机器上有 .NET 9 桌面运行时 + Node.js
