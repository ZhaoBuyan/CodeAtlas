# Code Atlas 使用说明

> 一句话：**把这个 exe 拖到任意代码库上，它会把代码扫成一张能看、能搜、能点开的图**——
> 帮你在陌生项目里快速搞清楚"有哪些东西、谁在用谁"。全部在你自己电脑上跑，代码不外传、不联网。

---

## 一、怎么开始（一分钟）

1. **拿到的是哪个文件**：`CodeAtlas.exe`（约 82 MB，免安装版）。放哪都行，桌面、D 盘、U 盘都可以。
2. **双击打开**。第一次启动会花几秒"释放内置引擎"（约 120 MB，只做这一次），然后弹出 **「项目设置」** 向导：

   | 步骤 | 你要做的 |
   | --- | --- |
   | ① 选项目 | 点「选择文件夹…」选一个代码目录（也可以选 `.dll` / `.exe` / `.jar`，它会先反编译再扫） |
   | ② 选语言 | 默认「自动」就行（所有代码语言都扫）；想快一点就只勾这个项目用的语言 |
   | ③ 分组规则 | 它**按目录结构自动草拟**一套"系统分组"（比如 Utils / Players / UI…）。不想要的取消勾选，名字和颜色可以改。 |

3. 点 **「保存并开跑」**。扫完地图会自动出现在同一个窗口里。
4. **以后再看这个项目：直接点「开跑」** —— 语言和分组规则都记着了，不用再配。

> 想给另一个项目配：点工具栏的 **「项目设置…」** 重新走一遍（每个项目各记各的）。

---

## 二、怎么看这张图

左边是筛选，中间是地图，右边是"详情"。

| 想干什么 | 怎么做 |
| --- | --- |
| 换一种看法 | 左下「视图」：**树形图**（面积=代码量）/ **树状列表** / **依赖图**（谁连谁）/ **依赖矩阵**（模块互相引用次数） |
| 按模块看 | 「分组方式」：系统（你草拟的规则）/ 目录 / 命名空间 / 平铺；「分组层级」1~4 层 |
| 找东西 | 顶部搜索框，**类型名和成员名都能搜**——比如搜 `OnPaint`，能直接找到"谁定义了这个方法" |
| 看某个东西 | 点地图上的方块 → 右边检查器显示：它是什么、在哪个文件哪一行、有哪些成员、依赖了谁、被谁依赖 |
| 看"谁引用谁" | 勾上「依赖高亮」，或者选中一个方块看它连到哪些 |
| 分清颜色 | 「着色」：按文件 / 按类型 / 按依赖深浅 |
| 太乱 | 「最小代码行」滑块滤掉小碎片；「类型类别」只留 class / interface 之类 |
| 分享某个视图 | 地址栏就是完整状态（`#q=OnPaint&v=graph`…），复制给别人能直接打开同样的画面 |

小提示：地图里**暗色方块**是当前的筛选/搜索之外的东西，不是出错。

---

## 三、可能会遇到的

- **Windows 弹"已保护你的电脑 / 未知发布者"**：因为这个 exe 没有买数字签名证书。点「更多信息」→「仍要运行」即可。
  （完全版是自包含的，不需要装 .NET；同目录的 `CodeAtlas-lite.exe` 是精简版，要求电脑上装了 .NET 9 和 Node.js，一般用不上。）
- **第一次启动慢几秒**：那是在释放内置引擎到 `%LocalAppData%\CodeAtlas\engine\`。**只做一次**；删掉那个目录它会自己重建。
- **你项目里的代码会被改动吗**：不会。它只读代码；分组规则默认写在 `%LocalAppData%\CodeAtlas\configs\` 里
  （向导第三步勾了"写进项目目录"才会在你项目里生成一个 `atlas.facets.json`）。
- **图上有些数字带"未知/歧义"**：它对依赖的判断是**按名字静态匹配**的，同名、动态调用这类情况它不装懂——
  界面上凡是没把握的都标出来了（未解析、歧义、置信度）。
- **想卸载**：删掉 exe；再删 `%LocalAppData%\CodeAtlas` 就彻底干净了。
- **扫不动 / 特别慢**：把「项目设置」里的语言勾少一点（只留这个项目真用的），大仓库会快很多。

---

## 四、它是什么、不是什么

**是**：一个"看图工具"。把你自己的代码库（或别人的开源库、反编译出来的程序集）扫成中间数据，
让你**自己翻、自己搜、自己点开看**——理解陌生代码的第一步。

**不是**：不是反编译器、不是 IDE、不是"AI 问答"。它不会假装权威：所有推断都标了来源和不确定的地方；
它也不会把代码上传到任何服务器。

支持的语言：C# / TypeScript / TSX / JavaScript / Java / Python / Kotlin / Lua / Go / Rust / C / C++ / PHP /
Swift / Scala / Shell / Zig / Solidity / OCaml / ReScript / TLA+ / SystemRDL / Emacs Lisp / Elixir（共 24 门），
外加 JSON / YAML / TOML / CSS / HTML 这些文件级格式（默认不扫，要在「项目设置 → 语言」里显式勾）。

---

## 给 AI 用（可选，但很省 token）

Code Atlas 自带一个 **MCP 服务**：接上以后，AI（Chatbox / Claude / Cursor…）能直接“查”这个代码库，
不用你把文件一个个塞进对话 —— **省几十倍 token，而且答案有出处**。两步：

1. 启动器里对这个项目**点一次「开跑」**；
2. 点工具栏上的 **「MCP 配置」** → 提示“已复制到剪贴板” → 把那段 JSON **粘进 AI 客户端的 MCP 配置**里 → 重启客户端。

接上之后可以这样问它：“改 `某个类` 会影响哪些地方”（影响面分析，多跳）、“先用 4000 token 讲清楚这个库”
（按预算导出骨架）、“谁定义了 `某个方法`” —— 一共 8 个查询工具。各客户端的**粘贴位置**和排错，见和本说明放在一起的《AI接入指南.md》。

不接 AI 也完全没关系：这个工具主要是给**你自己翻图**用的。

## 一个省时间的开关：增量

工具栏上有个 **「增量」** 勾选框（默认关 = 每次全量）。勾上之后重新扫描时**只重新解析你改过的文件**，
其余复用上次结果——改几个文件再看图，一两秒就出来。
（“谁引用谁”那部分仍会整体重算，所以大项目才明显；输出目录里会多一个缓存文件，删了也没事，下次自动重建。）
